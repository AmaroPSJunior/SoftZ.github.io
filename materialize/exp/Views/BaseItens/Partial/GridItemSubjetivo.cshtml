@model SLCONSTRUCT.UI.WEB.ViewModels.ItemSubjetivoVModel

@if (Model.listaItemsubjetivo.Count() > 0)
{
    <table id="table-pesq-habilidades" class="striped">
        <thead>
            <tr>
                <th class="largura-titulo-tr">Titulo</th>
                <th class="-acoes">Ações</th>
                <th><i class="fas fa-plus sl-navy-blue itemmacrodesafio" aria-hidden="true"></i></th>
            </tr>
        </thead>

        <tbody>

            @if (Model.listaItemsubjetivo.Count() > 0)
            {
                foreach (var item in Model.listaItemsubjetivo)
                {
                    <tr data-coditem="@item.coditem">
                        <td>@item.Titulo</td>
                        <td class="-acoes">
                            <i class="fas fa-pen-square alterar-item-macro" aria-hidden="true"></i>
                            <i class="fas fa-trash-alt exclui-item-macro" aria-hidden="true" title="Excluir"></i>
                        </td>
                        <td></td>
                    </tr>
                }
             }

        </tbody>

    </table>
}
else
{

    <div class="center paddingcollapse">
        <div class="">
            <span>NÃO HÁ QUESTÕES CADASTRADAS</span>
        </div>
        <div class="itemcollapse">
            <i class="fas fa-plus adicionar-item abre-modal-macrodesafio" aria-hidden="true"></i>Criar Questão
        </div>
    </div>
}



